# Channel-specific credentials are not configured on main.
# To use Telegram or another channel, merge the corresponding skill branch,
# which adds the required env vars and setup instructions.
# See .claude/skills/ for available channels.

# Telegram (added by skill/telegram)
TELEGRAM_BOT_TOKEN=
