// Channel self-registration barrel file.
// Skill branches add imports here when merged (e.g. `import './telegram.js'`).
// Each import triggers the channel module's registerChannel() call.
// A fresh main clone has no active channels — startup shows "No channels connected"
// until at least one channel skill is merged (e.g. `git merge upstream/skill/telegram`).

// discord

// gmail

// slack

// telegram

// whatsapp

// emacs
import './emacs.js';
